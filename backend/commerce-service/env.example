# MindfulMeals Commerce Service Environment Variables

# Server Configuration
NODE_ENV=development
PORT=3002

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=app_user
DB_PASSWORD=secure_password
DB_NAME=mindfulmeals

# Security Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002

# Feature Flags
ENABLE_GRAPHQL=true
ENABLE_REST_API=true

# Note: This service only handles app redirects and deep links
# No vendor API keys needed for current functionality
# Deep link integration coming soon
